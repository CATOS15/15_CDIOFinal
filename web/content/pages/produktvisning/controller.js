angular.module('CDIOFinal').controller('produktvisningController', ['$scope', 'produktvisningModel', 'produktvisningService', function ($scope, produktvisningModel, produktvisningService) {
    $scope.init = function(){
    };
    $scope.init();
}]);